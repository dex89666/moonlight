// Pre-generated canned responses for offline mode.
// Generate more realistic, multi-sentence canned responses
function makeLong(n:number, kind:string){
  const templates = [
    (i:number) => `${kind}: В этом отчёте я подробно рассматриваю ключевые аспекты и даю практические рекомендации. Ваш основной мотив — это стремление к развитию и самопознанию. В ближайшие месяцы советую сфокусироваться на коммуникациях и управлении энергией: небольшие ежедневные практики принесут значимый результат. (вариант #${i})`,
    (i:number) => `${kind}: Вы обладаете сильной интуицией и творческой жилкой. В отчёте мы рассматриваем афекты, которые формируют ваши решения, и даём конкретные шаги по улучшению отношений и карьеры. Особенное внимание уделите режиму дня и работе с вниманием. (вариант #${i})`,
    (i:number) => `${kind}: Анализ показывает баланс между личной инициативой и поддержкой окружения. Рекомендую развивать навыки планирования и просить помощи в важных проектах — это усилит ваши возможности. В эмоциональной сфере будьте готовы к периодам нестабильности; используйте дыхательные практики для стабилизации. (вариант #${i})`,
    (i:number) => `${kind}: Ваша жизненная матрица указывает на цикличность в ресурсах: периоды подъёма сменяются фазами восстановления. Мы разберём как перераспределять усилия, чтобы избежать выгорания. Маленькие ритуалы утром и вечером помогут закрепить позитивные изменения. (вариант #${i})`,
    (i:number) => `${kind}: Отчёт включает конкретные рекомендации по карьерным шагам, общению и личному развитию. Одна из главных задач — наработать устойчивые привычки и границы. Пробуйте вести дневник успехов и ставить небольшие измеримые цели на неделю. (вариант #${i})`
  ]
  const arr: string[] = []
  for(let i=1;i<=n;i++) arr.push(templates[i % templates.length](i))
  return arr
}

export const MATRIX_RESPONSES = makeLong(100, 'Матрица. Прогноз');
export const COMPAT_RESPONSES = makeLong(100, 'Совместимость. Оценка');
export const TAROT_RESPONSES = makeLong(100, 'Таро. Карта');
export const ZODIAC_RESPONSES = makeLong(100, 'Зодиак. Гороскоп');

export function pickDeterministic(key:string, arr:string[]){
  // simple hash
  let h=0; for(let i=0;i<key.length;i++){ h = ((h<<5)-h) + key.charCodeAt(i); h |= 0; }
  const idx = Math.abs(h) % arr.length;
  return arr[idx];
}

export function pickRandom(arr:string[]){
  return arr[Math.floor(Math.random()*arr.length)];
}
