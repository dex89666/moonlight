# LLM
OPENAI_API_KEY=
MODEL=gpt-4o-mini
# Gemini (Google) — if you use Gemini, set the key and optional endpoint
# GEMINI_API_KEY should contain the secret/token
# GEMINI_API_URL is the endpoint (optional). If you accidentally put the key here,
# the server will detect common key shapes and use it as a key.
GEMINI_API_KEY=
GEMINI_API_URL=

# квоты
FREE_MESSAGES_PER_DAY=2

# платежи
# Провайдер: telegram | stripe | none
PAYMENTS_PROVIDER=telegram

# Telegram bot settings
# Имя бота (как в @BotFather), с @ или без — не важно
TELEGRAM_BOT_USERNAME=@your_bot_here
# Токен бота (@BotFather) — вставьте в локальный `.env`, не в этот файл
TELEGRAM_BOT_TOKEN=YOUR_TELEGRAM_BOT_TOKEN_HERE
# Секрет для проверки вебхука Telegram (X-Telegram-Bot-Api-Secret-Token)
TELEGRAM_WEBHOOK_SECRET=YOUR_TELEGRAM_WEBHOOK_SECRET_HERE

# Stripe (если выберете stripe) — вставьте секрет в локальный `.env`
STRIPE_SECRET=YOUR_STRIPE_SECRET_HERE

# SECURITY: This example file must not contain real secrets. Put real keys in a
# local `.env` (which is in `.gitignore`) or in your deployment platform's secret
# manager (Vercel Environment Variables, Google Secret Manager, etc.).
